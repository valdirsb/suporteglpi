import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class LoginConfig extends Component {
    render() {
        return (
            <View>
                <Text> Configurações de Ligin </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
