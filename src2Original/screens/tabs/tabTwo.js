import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class tabTwo extends Component {
    render() {
        return (
            <View>
                <Text> texto Tab2 </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
